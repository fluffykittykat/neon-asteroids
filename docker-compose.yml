version: '3.8'

services:
  game:
    build: .
    container_name: neon-asteroids
    ports:
      - "8080:8080"
    environment:
      - VITE_GEMINI_API_KEY=${VITE_GEMINI_API_KEY}
    restart: unless-stopped
